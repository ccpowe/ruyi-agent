## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `ccpowe/ruyi-agent`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses a single domain context rooted at `CONTEXT.md`, with architectural decisions in `docs/adr/`. See `docs/agents/domain.md`.
